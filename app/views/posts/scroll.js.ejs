<% posts.forEach(function(post) { %>
	$("#main ol").append((function(){ /*
	<% include _post %>
	*/}).toString().split('\n').slice(1, -1).join('\n'));
<% }); %>

<% if (last) { %>
	finishedScrolling = true;
	$("#more_posts_marker").remove();
<% } %>

sizeTitles();