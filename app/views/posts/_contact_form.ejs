<p class="contact-form-explanation">We'll deliver your email to the poster, and if they choose, they'll respond to the email you provide below.</p>
<form action="<%= post.url() %>/send_email" method="post" class="contactForm group ajax">
	<p class="group email <%= currentUser ? 'confirmed-field' : '' %>">
		<label for="email">Email:</label>
		<% if (currentUser) { %>
			<span class="static-form-box"><%= currentUser.email %></span>
		<% } else { %>
			<input type="email" name="email" placeholder="yourname@something.upenn.edu" value="<%= email ? email : '' %>" />
		<% } %>
	</p>
	<p class="group text">
		<!-- <label for="text">Text:</label> -->
		<textarea name="email_text"></textarea>
	</p>
	<input type="submit" value="Send email" />
</form>